{
  "name": "jarvis",
  "version": "0.1.0",
  "type": "module",
  "description": "Personal AI assistant — Telegram bot powered by Claude CLI",
  "scripts": {
    "postinstall": "npm --prefix gui install",
    "build": "tsc && npm run build:gui",
    "build:server": "tsc",
    "build:gui": "npm --prefix gui run build",
    "dev": "tsc --watch",
    "dev:gui": "npm --prefix gui run dev",
    "start": "node dist/index.js",
    "chat": "node dist/entrypoints/chat.js",
    "dashboard": "node dist/entrypoints/dashboard.js"
  },
  "dependencies": {
    "dotenv": "^16.4.0",
    "telegraf": "^4.16.0",
    "ws": "^8.19.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/ws": "^8.18.1",
    "typescript": "^5.4.0"
  }
}
